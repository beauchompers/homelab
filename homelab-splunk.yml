---
- hosts: splunk
  become: yes
  vars_files:
    - extra-vars-splunk.yml
  tasks:
    - name: Setup Container Engine
      ansible.builtin.import_role:
        name: homelab-containers

    - name: Setup Splunk
      ansible.builtin.import_role:
        name: homelab-splunk
